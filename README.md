# marketing-assets

Generated social media images, hosted publicly so the Instagram Graph API can fetch them via URL.

Images are pushed here automatically by `scripts/image_host.py` before each Instagram post.
